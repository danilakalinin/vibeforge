# Screenshots

Referenced by `docs/index.html`, `docs/en.html`, `README.md` and `README.en.md`.
Keep the filenames — the pages link to them directly.

| File | What it shows | Used by |
|---|---|---|
| `editor.png` | Main window, Glass Light: editor left, colourised output right | landing (hero tab), both READMEs |
| `editor-dark.png` | Main window, Glass Dark | landing («Тёмная тема» tab) |
| `themes.png` | Settings with the theme dropdown open | landing, both READMEs |
| `ai-providers.png` | Settings with the AI provider dropdown open | landing, both READMEs |
| `snippet.png` | The save-snippet dialog over a blurred interface | both READMEs |

Capture at retina resolution and keep the window shadow. Downscale to ~1960px
wide before committing — that is still 2x for the widest slot on the page.
